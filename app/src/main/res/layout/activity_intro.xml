<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:background="@color/white"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".IntroActivity">

    <View
        android:id="@+id/introScreenContent"
        android:layout_width="375dp"
        android:layout_height="451dp"
        android:background="@drawable/intro_screen_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btnVerifyWebView"
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:text="Verify with Incode (1)"
        android:background="@drawable/btn_gradient"
        android:drawableLeft="@drawable/button_icon"
        android:drawablePadding="4dp"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:padding="10dp"
        android:fontFamily="@font/inter_bold"
        app:backgroundTint="@null"
        android:layout_marginBottom="31dp"
        app:layout_constraintBottom_toTopOf="@+id/btnVerifyInstantApp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="RtlHardcoded" /> <!-- drawableStart doesn't seem to work -->

    <Button
        android:id="@+id/btnVerifyInstantApp"
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:text="Verify with Incode (2)"
        android:background="@drawable/btn_gradient"
        android:drawableLeft="@drawable/button_icon"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:padding="10dp"
        android:fontFamily="@font/inter_bold"
        app:backgroundTint="@null"
        android:layout_marginBottom="31dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>